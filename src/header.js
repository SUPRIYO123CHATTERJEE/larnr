import TopHeader, {RightTop} from "./topheader";

export default function Header(props){
    return(
        <div>
          <TopHeader>
            <RightTop/>
          </TopHeader>

        </div>
    )
}